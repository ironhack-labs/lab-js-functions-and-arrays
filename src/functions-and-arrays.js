// Iteration 1 | Find the Maximum
function maxOfTwoNumbers(number1, number2) {
  if (number1 > number2) {
    return number1;
  } else if (number1 < number2) {
    return number2;
  } else {
    return number1;
  }
}

// Iteration 2 | Find the Longest Word
const words = [
  "mystery",
  "brother",
  "aviator",
  "crocodile",
  "pearl",
  "orchard",
  "crackpot",
];

function findLongestWord(longArray) {
    
    let palabraMasLarga = "";
  for (i = 0; i < longArray.length; i++) {
    if(longArray[i] > palabraMasLarga.length){
        
    }
  }
}

// Iteration 3 | Sum Numbers
const numbers = [6, 12, 1, 18, 13, 16, 2, 1, 8, 10];
function sumNumbers(arrayNumeros) {
    let resultado = 0;
  for (i = 0; i < arrayNumeros.length; i++) {
    resultado += arrayNumeros[i];
    
}
return resultado;
}

// Iteration 4 | Numbers Average
const numbers2 = [2, 6, 9, 10, 7, 4, 1, 9];

function averageNumbers(arrayAverage) {
    let result = 0
    for (i = 0; i < arrayAverage.length; i++) {
    result += arrayAverage[i] / arrayAverage.length;

}
    return result;

}

// Iteration 5 | Find Elements
const words2 = [
  "machine",
  "subset",
  "trouble",
  "starting",
  "matter",
  "eating",
  "truth",
  "disobedience",
];

function doesWordExist(arrayWords) {

    for (i = 0; i < arrayWords.length; i++) {

    }
}
